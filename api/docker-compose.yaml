version: '3.8'

services:
  postgres:
    image: pgvector/pgvector:pg17
    container_name: kigri-pg
    ports:
      - '5432:5432'
    environment:
      POSTGRES_DB: local
      POSTGRES_USER: local
      POSTGRES_PASSWORD: local
